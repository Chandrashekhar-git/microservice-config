spring:
  application:
    name: Config-Default
eureka:
  instance:
    prefer-ip-address: true
    #ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      deafultZone: http://localhost:8761/eureka
